import LoginPage from "./login";

export const metadata = {
    title: "Login | Jaya Network",
};

export default async function Page() {

    return (
        <>
            <LoginPage />
        </>
    );
}